# Preconfirmation Quarantine

These artifacts were generated on 2026-07-25 before the suite owner confirmation was resolved.

They are contractually premature and invalid for all official Phase 0 baseline uses because:

- the bound suite was still `pending_representativeness_confirmation`
- owner-bound confirmation metadata was unresolved
- referenced raw evidence lives outside the repository and is unavailable here

Do not use these quarantined files for:

- Phase 0 or Phase 1 Gate decisions
- metrics, scorecards, or denominator claims
- replay, regression comparison, or artifact reuse

The files are retained only as quarantined historical context and must stay `.invalid` until replaced by a genuinely preregistered baseline generated after preflight passes.
